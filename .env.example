# Backend Environment Variables Template
# Copy this file to .env and fill in your actual values
# ⚠️  CRITICAL: DO NOT commit .env to version control - contains sensitive keys!

# =============================================================================
# SUPABASE CONFIGURATION (Server-Side Only)
# =============================================================================

# Supabase Project URL
# Where to find: Supabase Dashboard → Settings → API → Project URL
SUPABASE_URL=https://your-project-id.supabase.co

# 🔴 SENSITIVE: Supabase Service Role Key (SERVER-ONLY)
# ⚠️  CRITICAL SECURITY WARNING:
# - This key has FULL DATABASE ACCESS - treat like a database password
# - NEVER expose this key to the client-side or frontend
# - NEVER commit this key to version control
# - Use this key ONLY in backend/server environments
# Where to find: Supabase Dashboard → Settings → API → Project API keys → service_role
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your_service_role_key_here

# =============================================================================
# AI SERVICE CONFIGURATION
# =============================================================================

# 🔴 SENSITIVE: Groq API Key
# ⚠️  This key provides access to AI services and may have usage costs
# Where to find: Groq Console → API Keys
GROQ_API_KEY=gsk_your_groq_api_key_here

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Application Environment
# Options: development, production, testing
ENVIRONMENT=development

# Logging Level
# Options: debug, info, warning, error
LOG_LEVEL=info

# Debug Mode (disable in production)
# Options: true, false
DEBUG=false

# =============================================================================
# DEPLOYMENT PLATFORM VARIABLES
# =============================================================================
# These are automatically set by deployment platforms - do not modify

# Render/Railway/Heroku automatically set PORT
# PORT=8000

# Some platforms set DATABASE_URL automatically
# DATABASE_URL=postgresql://user:pass@host:port/db

# =============================================================================
# SECURITY WARNINGS & BEST PRACTICES
# =============================================================================
#
# 🔴 HIGHLY SENSITIVE KEYS (Never expose):
# - SUPABASE_KEY (service_role) - Full database access
# - GROQ_API_KEY - AI service access with potential costs
#
# 🔒 SECURITY CHECKLIST:
# ✅ Add .env to .gitignore
# ✅ Use different keys for development/production
# ✅ Rotate keys regularly (quarterly recommended)
# ✅ Monitor API usage and costs
# ✅ Use environment variables in production (Render/Vercel dashboards)
# ✅ Never log or print these values
# ✅ Use HTTPS for all API communications
#
# 🚨 INCIDENT RESPONSE:
# If keys are accidentally exposed:
# 1. Immediately rotate all exposed keys
# 2. Check Supabase/Groq usage for unauthorized access
# 3. Update all deployment environments with new keys
# 4. Review git history and remove exposed keys if committed